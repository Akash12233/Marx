# ===========================================================================
# Marx Execution Management System Configuration
# ===========================================================================

# ---------------------------------------------------------------------------
# FIX Client Session
# ---------------------------------------------------------------------------
fix.sessionId=FIX_CLIENT_01
fix.host=127.0.0.1
fix.port=9876
fix.senderCompId=MARX_GW
fix.targetCompId=BROKER_FIX
fix.protocol=FIX.4.2
fix.heartbeatIntervalSecs=30
fix.reconnectDelaySecs=1
fix.maxReconnectAttempts=0
fix.isVenue=false

# ---------------------------------------------------------------------------
# OUCH Client Session
# ---------------------------------------------------------------------------
ouch.sessionId=OUCH_VENUE_01
ouch.host=127.0.0.1
ouch.port=9877
ouch.senderCompId=MARX_GW_OUCH
ouch.targetCompId=EXCHANGE_OUCH
ouch.protocol=OUCH
ouch.heartbeatIntervalSecs=1
ouch.reconnectDelaySecs=1
ouch.maxReconnectAttempts=0
ouch.isVenue=true

# ---------------------------------------------------------------------------
# Router Configuration
# ---------------------------------------------------------------------------
router.defaultVenueSessionId=OUCH_VENUE_01
